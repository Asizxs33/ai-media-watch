ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
PORT=3001

# Домены, которым разрешён доступ к API (через запятую).
# Укажи сюда адрес фронтенда на Vercel, например:
# CORS_ORIGINS=https://ai-media-watch.vercel.app
# Если оставить пустым — доступ разрешён всем (удобно для демо, но менее безопасно).
CORS_ORIGINS=
